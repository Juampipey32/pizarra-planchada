RewriteEngine On

# 1. Api requests stay as is
RewriteRule ^api/ - [L]

# 2. Root requests go to public/index.html
RewriteRule ^$ public/index.html [L]

# 3. Everything else goes to public/ folder
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ public/$1 [L]
